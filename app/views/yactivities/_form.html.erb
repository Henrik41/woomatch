<%= form_for @yactivity, :html => { :multipart => true } do |f| %>
  <% if @yactivity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yactivity.errors.count, "error") %> prohibited this yactivity from being saved:</h2>

      <ul>
      <% @yactivity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  
  <div class="field">
    <%= f.label :avatar %><br />
   <%= f.file_field :avatar %> 
  </div>
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
