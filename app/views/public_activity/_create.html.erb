<div class="section_2"> <%= image_tag 'logowoo2.png' %> </div>
<div class="section_3">
	<div class="section3_1">
created	<%= distance_of_time_in_words(@mytime, activity.trackable.created_at) %> ago
 	</div>
<%= link_to activity.owner.name, activity.owner %>
just added an activity 

<% if activity.trackable.user_id == current_user.id %>
<%= link_to activity.trackable.title, {:id => activity.trackable.id, :controller => 'activities', :action => 'show' } %>
<% else %>
<%= link_to activity.trackable.title, {:id => activity.trackable.id, :controller => 'general', :action => 'activity' } %>
<% end %>
</div>
<div class="section_4">
	<% if activity.trackable.user_id == current_user.id %>
<%= link_to image_tag(activity.trackable.avatar.url(:supermini)), {:id => activity.trackable.id, :controller => 'activities', :action => 'show' }  %>  
	<% else %>
				<%= link_to image_tag(activity.trackable.avatar.url(:supermini)), {:id => activity.trackable.id, :controller => 'general', :action => 'activity' } %>
				
					<% end %>

</div>