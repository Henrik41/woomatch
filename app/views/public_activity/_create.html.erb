<div class="section_2"> <%= image_tag 'logowoo2.png' %> </div>
<div class="section_3">
	<div class="section3_1">
created	<%= distance_of_time_in_words(@mytime, activity.trackable.created_at) %> ago
 	</div>
<%= link_to activity.owner.name, activity.owner %>
just added an activity 

<%= link_to activity.trackable.title %>
</div>
<div class="section_4">
	<% if activity.trackable.avatar.url(:supermini) %>
		<% if activity.trackable.user_id == current_user.id %>
		<%= link_to image_tag (activity.trackable.avatar.url(:supermini)), :controller => 'activities', :id => activity.trackable.id %>  
			
		<% else %>
				<%= link_to image_tag (activity.trackable.avatar.url(:supermini)), :controller => 'general', :action => 'activity', :id => activity.trackable.id %>  
		<% end %>
	
    <% else %>
 	No image available
<% end %>
</div>