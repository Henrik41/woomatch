$("#thepartial").hide();
$("#location_main").hide();

$("#location_change").empty().prepend('<%= @param %>').effect("highlight", {}, 1500);;
$("#select_button").empty().prepend('<% @activity.each do |a| %>				 <%= escape_javascript(render :partial => "/start/grid", :locals => {:a => a}) %> 				<% end %>').effect("highlight", {}, 1500);
